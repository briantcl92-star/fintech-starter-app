// ===== SDK EXAMPLE =====
// for Crossmint SDK users
// wallet create SDK reference: https://docs.crossmint.com/sdk-reference/wallets/overview#transfer
import { CrossmintWallets, createCrossmint } from "@crossmint/wallets-sdk";

const crossmint = createCrossmint({
    apiKey: "<your-client-OR-server-api-key>", // ⚠️ Replace placeholder with actual key
    jwt: "<your-jwt>", // 
});

const crossmintWallets = CrossmintWallets.from(crossmint);

// ⚠️ COMMON ERROR: Invalid chain name - check docs for supported chains (e.g., "ethereum", "polygon")
// ⚠️ COMMON ERROR: Email format validation - ensure valid email syntax
const wallet = await crossmintWallets.getOrCreateWallet({
    chain: "<your-chain>", // ⚠️ Must be exact chain identifier (case-sensitive)
    signer: {
        type: "email", // ⚠️ Other types: "passkey", "phone", "external-wallet"
        email: "<your-email>", // 
    },
});

console.log(wallet.address); // ⚠️ May be undefined if wallet creation failed

// transaction create SDK reference: https://docs.crossmint.com/sdk-reference/wallets/overview#transfer
// ⚠️ COMMON ERROR: Invalid recipient address format (must match chain's address format)
// ⚠️ COMMON ERROR: Token symbol case-sensitivity ("USDC" vs "usdc")
// ⚠️ INSUFFICIENT BALANCE: Transaction fails if wallet lacks funds or gas
const transaction = await wallet.send(
    recipient, // ⚠️ Must be valid address for the specified chain
    "usdc",    // ⚠️ Token must exist on the chain and wallet must have balance
    "100"      // ⚠️ Amount in token units 
);

console.log(transaction.explorerLink); // 

// 💡 BEST PRACTICE: Add error handling
// try {
//   const transaction = await wallet.send(recipient, "usdc", "100");
//   console.log(transaction.explorerLink);
// } catch (error) {
//   console.error("Transaction failed:", error.message);
//   // Handle specific errors: insufficient balance, invalid address, network issues
// }


# ===== API EXAMPLE =====
# for Crossmint API users
# wallet create API reference: https://docs.crossmint.com/api-reference/wallets/create-wallet
import requests

# ⚠️ ENVIRONMENT ERROR: Using staging URL - change to production for live apps
# Production URL: "https://www.crossmint.com/api/2025-06-09/wallets"
url = "https://staging.crossmint.com/api/2025-06-09/wallets"

payload = {
    # ⚠️ COMMON ERROR: Invalid chainType - must be "evm", "solana", or other supported types
    "chainType": "evm",
    
    # ⚠️ WALLET TYPE: "smart" enables gasless transactions but requires admin signer
    # Alternative: "custodial" (simpler) or "non-custodial" (user controls keys)
    "type": "smart",
    
    "config": { 
        "adminSigner": {
            # ⚠️ COMMON ERROR: Wrong signer type for use case
            # Types: "external-wallet", "passkey", "api-key", "email", "phone"
            "type": "external-wallet",
            
            # ⚠️ VALIDATION ERROR: Must be valid EVM address (0x... format, 42 chars)
            # ⚠️ SECURITY: This address has admin control over the wallet
            "address": "0x1234567890123456789012345678901234567890"
        } 
    },
    
    # ⚠️ OWNER FORMAT: Must be "email:user@example.com" or "phone:+1234567890"
    # ⚠️ COMMON ERROR: Missing prefix (email: or phone:) causes 400 error
    "owner": "email:user@example.com"
}

headers = {
    "X-API-KEY": "<x-api-key>",  # ⚠️ Replace with actual key
    "Content-Type": "application/json"
}

# ⚠️ NO ERROR HANDLING: Should check response.status_code before using data
response = requests.post(url, json=payload, headers=headers)

# ⚠️ JSON DECODE ERROR: Fails if response isn't valid JSON (e.g., 500 error HTML)
print(response.json())

# create transaction API reference: https://docs.crossmint.com/api-reference/wallets/create-transaction
import requests

# ⚠️ PATH PARAMETER: {walletLocator} must be replaced with actual wallet ID or address
# Format: wallet ID (UUID) OR "evm:0x..." OR "solana:..."
url = "https://staging.crossmint.com/api/2025-06-09/wallets/{walletLocator}/transactions"

payload = { 
    "params": {
        # ⚠️ CRITICAL ERROR: "<string>" placeholder - must be actual transaction data
        # Format depends on chain: EVM (calldata), Solana (serialized transaction)
        # ⚠️ ENCODING: Must be hex-encoded for EVM, base64 for Solana
        "transaction": "<string>",
        
        # ⚠️ ARRAY FORMAT ERROR: Should be array of strings, not array of arrays
        # ⚠️ AUTHORIZATION: Must include signers with permission to execute transaction
        # ⚠️ SIGNER FORMAT: "type:identifier" (e.g., "email:user@example.com")
        "requiredSigners": [
            ["passkey:cWtP7gmZbd98HbKUuGXx5Q",  # ⚠️ This nested array is wrong
             "api-key:123456789", 
             "external-wallet:0x1234567890123456789012345678901234567890", 
             "email:test@example.com", 
             "phone:+1234567890"]
        ],
        
        # ⚠️ TYPE ERROR: Should be string, not array
        # ⚠️ PERMISSION ERROR: This signer must match one of requiredSigners
        # ⚠️ AUTHENTICATION: Signer must be authenticated via API key or JWT
        "signer": [
            "passkey:cWtP7gmZbd98HbKUuGXx5Q",  # ⚠️ This should not be an array
            "api-key:123456789", 
            "external-wallet:0x1234567890123456789012345678901234567890", 
            "email:test@example.com", 
            "phone:+1234567890"
        ]
    } 
}

# ⚠️ CORRECT FORMAT SHOULD BE:
# "requiredSigners": ["email:test@example.com"],  # Array of strings
# "signer": "email:test@example.com"              # Single string

headers = {
    "X-API-KEY": "<x-api-key>",  # ⚠️ Replace placeholder
    "Content-Type": "application/json"
}

response = requests.post(url, json=payload, headers=headers)
print(response.json())
